{
  "name": "@serverlab/electron",
  "version": "3.0.0-beta.18",
  "description": "Electron main + preload process for ServerLab MC",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc --noEmit --watch",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@serverlab/shared": "*",
    "electron-updater": "^6.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "electron": "^43.2.0",
    "typescript": "^5.5.0"
  }
}
